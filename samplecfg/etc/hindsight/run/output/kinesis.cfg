filename            = "kinesis_single.lua"
message_matcher     = "TRUE"
ticker_interval     = 0

streamName              = "name-of-stream"
-- partitionField       = "Uuid"
credentialProvider      = "CHAIN" -- Use default credential provider chain
-- roleArn              = nil

clientConfig = {
    region = "name-of-region"
}

encoder_module = "encoders.heka.protobuf"
